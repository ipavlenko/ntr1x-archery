{% extends 'NTR1XLayoutBundle:private-layout.html.twig' %}

{% block editor %}

	<h1>Страницы</h1>

	<section>
		<nav class="navbar">
			<a class="btn btn-primary navbar-btn"
				href="{{ path('admin-layout-pages-create') }}"
			>Добавить</a>
		</nav>
	</section>

	<section>
		<table class="table table-condensed">
			<thead>
				<tr>
					<th></th>
					<th>Имя</th>
					<th>Заголовок</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for item in pages %}
				<tr>
					<td>{{ loop.index }}</td>
					<td>{{ item.name|e }}</td>
					<td>{{ item.title|e|first }}</td>
					<td>
						<a class="btn btn-xs btn-default" href="{{ path('admin-layout-pages-update', {
							id: item.id,
						}) }}">Редактировать</a>
						<a class="btn btn-xs btn-danger" href="{{ path('admin-layout-pages-do-delete', {
							id: item.id,
						}) }}">Удалить</a>
					</td>
				</tr>
				{% endfor %}
			</tbody>
		</table>
	</section>

{% endblock %}