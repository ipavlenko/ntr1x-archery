Core = window.Core || {};

(function(Vue, $, Core) {

    Core.WidgetMixin = {
        props: {
            data: Object,
            params: Object,
        }
    }
    
})(Vue, jQuery, Core, undefined);
