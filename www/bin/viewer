#!/usr/bin/env node

const launch = require('../launch')
const setup = require('../setup')
const routes = require('../routes/viewer.js')

const config = require('config')

const c = config.get('viewer')

launch(c, setup(c, routes))
